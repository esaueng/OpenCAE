# Local SQLite

The local SQLite schema is owned by `@opencae/db` (`libs/opencae-db/src/index.ts`); run `pnpm db:migrate` to create it and `pnpm db:seed` to seed demo data. The database file lives at `data/sqlite/opencae.local.sqlite`.
