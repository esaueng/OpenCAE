# Cantilever Demo

The Cantilever Demo is a built-in sample project for a beam fixed at one end and loaded at the free end.

It can be loaded as a static stress or dynamic structural study. The sample includes face selections for the fixed end and load end, material assignment, load setup, mesh summary generation, local solve support, result playback for dynamic runs, and report artifacts.
