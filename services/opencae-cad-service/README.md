# OpenCAE CAD Service

Mock CAD inspection boundary for the local MVP. It returns stable CAD topology references and writes display metadata artifacts. Future Open CASCADE work should replace this service without changing study bindings.
