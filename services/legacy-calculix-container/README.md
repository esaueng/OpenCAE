# Legacy CalculiX Container

This directory is a quarantine marker for the removed Cloudflare FEA container. It is intentionally not a package, has no Dockerfile, and is not referenced by production scripts or Worker bindings.

Historical project files may still contain provenance from this backend. Those results are read-only history and must be recomputed with OpenCAE Core Cloud before being treated as production FEA.
