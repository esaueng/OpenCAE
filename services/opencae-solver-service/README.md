# OpenCAE Solver Service

Mock linear static solver boundary for the local MVP. It writes fake solver input, logs, result fields, and a summary artifact. Native solver integrations belong behind this contract later.
