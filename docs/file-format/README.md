# File Format

The scaffold stores mock geometry, mesh, result, and report artifacts under `data/artifacts`. JSON artifacts are intentionally simple and stable so future native integrations can write richer data behind the same references.
