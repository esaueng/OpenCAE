# Local Development

Run the local app with:

```bash
pnpm install
pnpm dev
```

`pnpm dev` starts the API on port `4317` and the web app on port `5173`. The API creates and seeds the local SQLite database if needed.
