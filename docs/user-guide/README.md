# User Guide

Start with **Load Sample Project**. OpenCAE loads the Bracket Demo, shows the 3D model, guides setup through Model, Material, Supports, Loads, Mesh, Run, Results, and Report steps, then displays mock stress results directly on the model.
